org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Kotlin's incremental compiler can crash when the project lives on a
# different Windows drive letter than the Gradle/pub caches (it tries to
# compute a relative path across drive roots, which throws). Disabling
# incremental compilation avoids that crash; see:
# https://youtrack.jetbrains.com/issue/KT-58547
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
