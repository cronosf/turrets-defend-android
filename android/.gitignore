/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Not used (no Firebase Gradle plugin applied — see build.gradle.kts/
# settings.gradle.kts) and contains a Google API key shared across other
# apps' projects, so it stays out of git if it's ever regenerated locally.
app/google-services.json
